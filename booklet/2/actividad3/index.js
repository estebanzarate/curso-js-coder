let num = parseInt(prompt('Ingrese un número'));

if(num >= 1 && num <= 4) {
    if(num === 1) alert('Elegiste a Homero');
    if(num === 2) alert('Elegiste a Marge');
    if(num === 3) alert('Elegiste a Bart');
    if(num === 4) alert('Elegiste a Lisa');
}